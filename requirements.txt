# Core
numpy>=1.24
opencv-python>=4.8
PyYAML>=6.0
tqdm>=4.66
requests>=2.31
scipy>=1.10           # scripts/02_extract_smd_frames.py -- SMD's .mat ground truth

# Optional per-source downloaders (install only what you need)
gdown>=5.1            # Google Drive hosted sets (DUO, SUIM, SMD)
kaggle>=1.6           # Kaggle mirrors (TrashCan)
roboflow>=1.1         # Brackish
fathomnet>=1.2        # FathomNet API

# Stage 4: WaterNet enhancement (models/waternet_checkpoint/ is a TF1-era
# checkpoint; [and-cuda] pulls matching cuDNN/cuBLAS wheels -- without it TF
# silently runs on CPU, ~8x slower for this model). CPU-only machines can drop
# the extra and just use `tensorflow`.
tensorflow[and-cuda]>=2.16

# Stage 5 (fine-tuning) is intentionally NOT listed here -- see
# requirements-train.txt and the "Setup" section of the README for why it
# needs a separate venv, not an entry in this one.
#
# Later stages (not needed for download/unify/enhance/train)
# (export needs onnx/tensorrt -- added when scripts/06_export.py exists)
